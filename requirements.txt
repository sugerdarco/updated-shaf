# Core numerics. The prefix tree, the sheaf reconciler and every Stage 5/6/7
# transcription are numpy-only; it was imported everywhere but never declared.
numpy>=1.24

torch>=2.2
transformers>=4.42
accelerate>=0.30
pyyaml>=6.0

# dataset/download_datasets.py and dataset/download_full_datasets.py
datasets>=2.19

pytest>=8.0
